# Laravel PHP Framework

## Installation

* `npm install` 
* `composer install`
* setting .env


* `npm run dev` - for develop 
* `npm run prod` - for production
* `npm run migrate`
* `npm run serve`

<br>

## Generate SiteMap

* `php artisan sitemap:generate`
